#include<stdio.h>
#include<conio.h>
main()
{
 int a,b;
 clrscr();
 printf("enter number a :");
 scanf("%d",&a);
 printf("enter number b:");
 scanf("%d",&b);
 if (a>=9)
  {
       printf("maximum number");
  }
 else
  {
      printf("minimum number ");
  }
 getch();
}